package org.youe;

import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public class Main {
    public static final String TIME_FORMAT_HOURS_MINUTES = "HH:mm";

    public static void main(String[] args) {
        /** string */
        // String trueSql = " AND r.status = 1 AND r.online = 1 ";
        // System.out.println(trueSql.concat(""));
        // System.out.println("Hello world!");
        
        /** time */
        // LocalTime time = LocalTime.now();
        // DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern(TIME_FORMAT_HOURS_MINUTES);
        // System.out.println(dateTimeFormatter.format(time));
    }
}